# 自动标注工具使用说明

一个 labelimg 风格的自动标注 + 人工修正工具。用你训练好的 YOLOv8 模型
（`SLM-AFPN-C2f_SCConv`，单类别 `mosquito`）对某个文件夹下的**所有图片和视频**批量打标签，
在画布上查看效果，并像 labelimg 一样手动修正错误标签，最后保存为 YOLO 格式标签。

## 启动

双击 **`start_label_tool.bat`**，或在命令行运行：

```
D:\minicoonda\envs\yolov8\python.exe auto_label_tool.py
```

> 使用的环境：`D:\minicoonda\envs\yolov8`（torch CUDA + 自定义 ultralytics fork 依赖已齐）。
> 脚本会自动使用本目录下的 custom `ultralytics`（含 SLM-AFPN / C2f_SCConv 等模块），
> 无需额外配置。

## 操作步骤

1. **选择模型**：默认已自动选中 `训练\SLM-AFPN-C2f_SCConv\weights\best.pt`，可点「选择模型」更换。
2. **选择文件夹**：选择存放图片/视频的文件夹（自动递归扫描 `jpg/png/bmp/...` 和 `mp4/avi/...`）。
3. **批量标注**：设置好置信度阈值（默认 0.25）、IoU（0.5）、视频帧间隔（默认 1，即逐帧）后点击，自动对全部文件推理。
4. **浏览查看**：左侧文件列表切换文件，画布上显示检测框（类别 + 置信度）。
5. **修正标签**：见下方「编辑操作」。
6. **保存全部**：把所有修正结果写为 YOLO 格式标签。
7. **导出可视化**（可选）：把带框的图片/视频输出到 `output/`。

## 编辑操作（labelimg 风格）

| 操作 | 方法 |
| --- | --- |
| 新建框 | 在空白处按住**左键拖动** |
| 选中/移动框 | 单击或**左键拖动框体** |
| 调整大小 | 选中后**拖动四角手柄** |
| 删除框 | 选中后按 **Delete**，或**右键**框 |
| 修改类别 | 选中后右侧「选中框信息 → 类别」下拉选择 |

**视图**：

| 操作 | 方法 |
| --- | --- |
| 缩放 | 鼠标**滚轮** |
| 平移 | **Space + 左键拖动**，或**中键拖动** |
| 适应窗口 | 按 **F** |

## 继续之前的操作

再次打开工具并选择**同一个文件夹**时，会自动读取 `labels/` 里上次保存的标签并恢复到画布上，
你可以直接接着上次的修正继续。也可以随时点工具栏【读取已有标签】手动重新载入。

> 说明：保存为标准 YOLO 格式时不带置信度，因此重新载入的框会显示为「手动」；
> 坐标与类别信息均完整保留。

## 视频逐帧修正

右侧「视频帧导航」提供**上一帧 / 下一帧 / 播放 / 拖动进度条**。可在每一帧上增删改框，
保存时逐帧写入标签。

## 输出格式

- 图片 `foo.jpg` → `labels/foo.txt`
- 视频 `bar.mp4` → `labels/bar/frame_000123.txt`（每帧一个）
- 类别清单 → `labels/classes.txt`
- 可视化 → `output/`（图片为 `.jpg`，视频为 `.mp4`）

标签为 YOLO 格式，每行 `class_id cx cy w h`（中心点 + 宽高，均归一化到 0~1）。

若取消勾选「保存到 labels/ 子文件夹」，则标签会写到每个图片/视频同目录下。

## 导出训练集（继续训练用）

修正完标签后，点工具栏【导出训练集】：

1. 会先把当前修正结果落盘（等同【保存全部】）。
2. 选择输出目录（默认建议新建空目录；若直接选了源目录会自动转到其 `yolo_dataset/` 子目录）。
3. 输入验证集占比（默认 0.1）。

生成 ultralytics 可直接训练的目录结构：

```
<输出目录>/
  images/train/*.jpg
  images/val/*.jpg
  labels/train/*.txt
  labels/val/*.txt
  dataset.yaml      # path / train / val / names 已写好
```

- 图片连同其标签按比例随机划分到 train / val。
- 视频里**有标签的帧会被抽取成图片**（`视频名__frame_000123.jpg`）一并加入训练集。
- 命令行也可用：`python export_dataset.py --src <已标注文件夹> --out <输出目录> --val 0.1`

之后用 `dataset.yaml` 直接训练即可：

```bash
yolo train data=<输出目录>/dataset.yaml model=yolov8-AFPN-P2345-Custom.yaml epochs=200
```

## 备注

- 手动新建的框置信度显示为「手动」。
- 批量推理在后台线程进行，进度显示在右下角状态栏；推理期间可等待，完成后自动刷新画布。
- 若视频较长，建议适当调大「视频帧间隔」加快速度。
